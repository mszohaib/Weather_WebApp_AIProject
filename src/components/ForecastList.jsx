export default function ForecastList() {
  //This will hold the 5 day forestCast card to the user and should get the data and display it to the user
  //Later , it will use an array of 5 day objects to display real data
  return (
    <div>
      <h2>Forecast cards(Component) </h2>
      Forcast List (All the data should be here SHOWN ) days = [ {}, {}, {}, {},
      ,{},{}]
    </div>
  );
}
